# Import the Tkinter window object from the visual_interface module
from visual_interface import window

# The entry point of the program
if __name__ == "__main__":
    # Start the Tkinter main event loop to display the GUI window
    window.mainloop()
